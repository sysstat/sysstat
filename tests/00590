. tests/variables
if [ "$HAVE_PCP" = "y" ]; then
	rm -f tests/pcpar.*
	LC_ALL=C PCP_ARCHIVE_VERSION=2 ./sadf -l -O pcparchive=tests/pcpar tests/data.tmp -C -- -A
else
	echo Skipped
	touch tests/SKIPPED
fi
